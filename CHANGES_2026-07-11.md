# CHANGES 2026-07-11 — Paper defect remediation (Hroar-directed paper-reorg sweep)

Source audit: `qhots_v7_calc/outputs/research/paper_reorg/audits/identity-sum/final_verdict.md`
and `consolidated.json` (verdict CONDITIONAL, 20 unique ranked issues, 0 blocking, 8 major,
8 minor, 5 info). Pristine original preserved before any edit at:
`/media/qhots/WD_BLACK/claude/qhots/archive/paper/2026-07-11_v110_reorg/siblings/identity-sum/identity_sum_paper_ORIGINAL_2026-07-11.tex`

Scope: LaTeX-only, conservative. No substantive numerical/experimental result or core
claim was changed. CITATION.cff and .zenodo.json (a concurrent agent's DOI/citation work)
were not touched, staged, or reverted.

## Issues fixed (13 of 20)

- **ISSUE-1 (major, metric fork):** Abstract/tab:metrics/Conclusion/tab:app-metrics said
  "2,066+ tests / 963 simulations"; sec:sefirot-platform/sec:related-autonomous said
  "2,162+ tests / 966 simulations". Reconciled to the single later snapshot (2,162+/966,
  git-dated 2026-02-24 via commit a252f55) at all four stale locations (abstract, Table
  tab:metrics, Conclusion, Table tab:app-metrics); added "(as of 2026-02-24)"
  snapshot labels to both table captions and the tab:metrics intro sentence.
- **ISSUE-2 (major, cited-artifact mismatch):** app:sefirot-spec cited
  `sefirot_architecture.json` for the 91/113/34 figures, but that tracked file's actual
  contents (57/113/31, per the audit) contradict it. Reworded the "Architecture file"
  entry to attribute the 91/113/34 headline figures to `SEFIROT.md` (which the audit
  confirms matches) and describe the JSON as a periodically-regenerated snapshot that
  may lag. No numbers changed, only the provenance pointer.
- **ISSUE-3 (major, abstract overclaim):** Abstract's present-tense "We demonstrate that
  ... reduces both failure modes" (body support is N=1 narrative incidents only; the
  10-25% figure is a companion-paper prediction). Changed to "We argue ... can reduce
  both failure modes, illustrated here by case-study incidents; systematic benchmark
  validation is proposed as future work (Sec. experiments)."
- **ISSUE-4 (major, convergence claim internal inconsistency):** sec:convergent-architecture
  asserted "The convergence emerged from the problem structure, not from instruction" while
  app:relay concedes it is "mediated by pattern transmission." Reconciled: softened "The
  most significant empirical finding" -> "A significant empirical finding"; added an
  explicit non-controlled-result caveat naming the shared-researcher confound, cross-
  referencing Limitations item 5 and Appendix (app:relay); reworded the closing sentence
  to state the shared-channel and problem-structure explanations "cannot be fully
  disentangled without an independent-architect control" instead of denying the confound.
  Extended Limitations item 5 to name this same caveat explicitly.
- **ISSUE-5 (major, title-abstract disconnect):** "The Identity Sum" (the paper's
  namesake, defined at eq:system-identity/Sec 5) was absent from the abstract. Added one
  abstract sentence naming the Identity Sum and its convergent validation ratio.
- **ISSUE-7 (major, inconsistent speculative disclaimer):** Sec. 5 "Great Attractors: Why
  Truth Attracts" used declarative gravitational-attractor language with no disclaimer,
  unlike its Sec. 10 physics-connection twin (which is boxed as "Structural analogy
  only"). Added a matching boxed "Structural analogy only" remark at the top of Sec. 5,
  and extended Limitations item 4 to name Sec. 5's attractor metaphor alongside Sec. 10's
  physics connection as the most speculative components.
- **ISSUE-8 (major, speculation interleaved across 4 sections):** Added one paragraph
  after the Introduction's paper-contents list pointing the reader to every
  self-labeled speculative section (Sec. 5 attractors, Sec. 9 math-foundations
  "Speculative Extensions", Sec. 10 physics-connection) instead of leaving the reader to
  discover disclaimers section-by-section. No section was physically moved/restructured
  (out of conservative LaTeX-only scope).
- **ISSUE-9 (minor, untested graph-ratio conjecture):** sec:graph-ratio's phi conjecture
  was never checked against the paper's own Chesed claim counts, and the gap was not
  flagged. Added an "Open empirical gap" note explaining edge counts |E_n| are not
  currently tracked separately from claim counts |V_n|, so rho_n could not be computed,
  and flagging instrumentation + the check as explicit future work (did not fabricate an
  edge count — see "Left for the director" below).
- **ISSUE-10 (minor, duration mismatch):** Abstract/sec:related-autonomous/sec:experimental-
  conditions said "approximately one year" while Table tab:experimental-phases sums to
  ~16 months (10+2+4). Changed all three occurrences to "approximately 16 months,"
  computed directly from the paper's own table (no new external claim).
- **ISSUE-11 (minor, Conclusion drops conditional framing):** Conclusion presented the
  truth-attractor convergence as an unconditional "central finding." Restored the
  conditional framing by naming the three enforceable conditions from Sec.
  \ref{sec:convergence} (non-demotion, bounded MUD growth, honest-negatives floor)
  inline. Also added the 2026-02-24 snapshot label to the Conclusion's metrics restated
  there (963/2,066+ -> 966/2,162+, consistent with ISSUE-1).
- **ISSUE-12 (minor, bare "sum converges" assertion):** sec:no-single-identity asserted
  "The sum converges" two subsections before any supporting argument. Added a forward
  pointer to the Convergence Proof (Sketch) sub-section and its scope restriction.
- **ISSUE-14 (minor, unresolvable "companion data release" pointer):** app:union deferred
  the Union-to-Sefirot node mapping to an unnamed "companion data release" that the audit
  confirmed does not exist in this repo. Replaced with an honest statement that the full
  mapping lives in the separate, now-dormant Union project archive and is not included as
  a companion artifact with this submission. The underlying claim (12/13 patterns ported,
  76 validating tests) was not touched.
- **ISSUE-18 (info, unused bibitem):** `\bibitem{penrose2010cycles}` was defined but never
  `\cite`'d anywhere in the body. Removed the unused bibitem (verified via cite/bibitem
  regex diff before and after; no citation in the body referenced this key).

## Left for the director

- **ISSUE-6 (major, traceability):** Table tab:arc-metrics's four physics rows (alpha =
  28/3837, G SEMI-DERIVED, S_dressed, Weak Mixing Angle) carry no in-text phase/sim
  pointer. The audit found the underlying numbers ARE consistent with
  qhots_v7_calc canon (PHASE_INDEX Phase 402-404/420-421, FORMULAS.md sims
  1469+1473, G per Phase 28), but adding specific phase/sim citations into this table
  requires cross-referencing the qhots_v7_calc canonical sources at director-review time
  to avoid citing a stale/wrong phase number — left unfixed per the FENCE (do not guess
  physics-provenance pointers).
- **ISSUE-13 (minor, stale committed PDF):** Resolved as a side effect of this pass's
  mandatory rebuild (see Build Result below) — noting here only because the audit filed
  it as a distinct issue; no text change was needed beyond the rebuild + commit.
- **ISSUE-15 (minor, 11 hbox warnings) / ISSUE-16 (minor, font-shape warning):** Cosmetic
  LaTeX typesetting warnings (Overfull/Underfull hbox in tab:convergence/tab:arc-metrics
  regions; `T1/cmr/m/scit` font-shape substitution warning near line ~774). Confirmed
  still present in the rebuilt log, non-blocking, do not affect PDF generation or
  readability. Left unfixed: table-width/line-break tuning risks introducing new spacing
  regressions in a conservative pass and was judged lower value than the majors above.
- **ISSUE-17 (info, stale sibling mirrors):** `qhots_v7_calc/paper/identity_sum_paper.tex`
  and `lexis-heart321/cs-papers/identity-sum/paper/identity_sum_paper.tex` are stale
  pre-ORCID mirror copies. Cross-repo sync is explicitly director territory per the
  paper-reorg sweep's own scoping (each repo's own agent fixes its own tree) —
  not touched from this repo.
- **ISSUE-19 (info, self-report-only claims) / ISSUE-20 (info, abstract scope
  over-delivery):** Both are info-severity per the audit and already substantially
  covered by existing hedges (Limitations item 1's N=1 caveat; Sec. 9/10's own
  "Speculative Extensions" / "structural analogy only" self-labeling). No text change
  made; judged not to need action beyond what ISSUE-7/ISSUE-8 fixes above already added.

## Build result

Ran `pdflatex -interaction=nonstopmode -halt-on-error identity_sum_paper.tex` **three**
times from `/home/qhots/Codex/Projects/identity-sum/paper/` (consistently from the
`paper/` directory, so all build artifacts — `.aux`/`.log`/`.out`/`.toc` — land in
`paper/` next to the `.tex`, matching the pre-existing tracked-PDF location). Two runs
were the minimum requested; a third was run because run 2's log still emitted "Label(s)
may have changed. Rerun to get cross-references right." Run 3 produced no such warning.

- Exit code: 0 (all three runs)
- Pages: 26 (stable across runs 2 and 3; up from the pre-edit 24 pages the audit
  recorded, due to added remarks/paragraphs)
- LaTeX errors: 0
- Undefined references: 0
- Undefined citations: 0
- Remaining warnings: 1 cosmetic font-shape warning (`T1/cmr/m/scit` undefined,
  substituted `scsl`) + the pre-existing Overfull/Underfront hbox warnings (ISSUE-15/16,
  left unfixed, see above)

`paper/identity_sum_paper.pdf` was rebuilt in place and is committed alongside the
`.tex` and this file.

## Files touched

- `paper/identity_sum_paper.tex` (edited)
- `paper/identity_sum_paper.pdf` (rebuilt, committed)
- `CHANGES_2026-07-11.md` (this file, new)

**Not touched:** `CITATION.cff`, `.zenodo.json` (concurrent agent's DOI/citation work —
left exactly as found, not staged, not reverted).
